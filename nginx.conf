server {
    listen 80;
    server_name _;

    # Serve the React application
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Handle React Router (always fall back to index.html)
        try_files $uri $uri/ /index.html;
    }
}
